// display.c — placeholder, sin implementacion propia.
//
// La logica de dibujo real (limpiar_pantalla, poner_pixel,
// dibujar_sprite, dibujar_texto) esta declarada en
// software/grupo_K_juegos/comun/framebuffer.h y es compartida por los
// 4 juegos -- su implementacion (.c) todavia no se ha escrito, queda
// pendiente de que el equipo decida la resolucion final de cada juego
// (ver README.md de esta carpeta, "problema del tamaño del framebuffer").
//
// Este archivo se deja como referencia para no duplicar esa logica
// aqui: por ahora solo incluye las direcciones de hardware.

#include "display.h"
